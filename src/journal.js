// business logic

export default function Entry(title, entry){
  this.title = title;
  this.entry = entry;
};

// Entry.prototype.countWords = function() {
//   for (let i = 0; i < 10; i++) {
//   }
// };

Entry.prototype.Vowel = function() {
  if (input === 'a' || input === 'e' || input === 'i' || input === 'o' || input === 'u' || input === 'y') {
    return true;
  } else {
    return false;
  }
}

// const wordCount() {
//   let wordCount = " "
//   if wordCount = 
// }

function wordCount {
  return trim()
}


const str = 'I am a human'
console.log(str.includes('a'))